# mcpradar-rule-deprecated

Community detection rule for MCPRadar — detects deprecated/legacy API patterns.

## Rule

| Rule ID | Title | Severity |
|---------|-------|----------|
| X002 | Deprecated/legacy API pattern detection | LOW |

Detects: `v1` API references, `deprecated`/`legacy`/`obsolete` keywords, versioned API paths (`/v0/`, `/v1/`).

## Install

pip install mcpradar-rule-deprecated
